# Bump notes

## 2026-08-28 — Commerce D1 storefront authority

- Finalized the successful first genuine Stripe TEST acceptance for preserved order `ord_e47b94a4-4252-438b-8ca7-c47470029940` and Session `cs_test_a1vXUK8hmsaKfXmciNGnU25zL1PdhbkyjFJ0KgDRoHFUkaYvROZiWoG5OC`: one signed `checkout.session.completed` receipt moved the exact CAD 15.00 TEST order to paid while fulfillment remained disabled and no Printful order was created.
- Closed `stripe_test_checkout_enabled`, removed its temporary product/variant selectors, restored `variant-5019554081` to non-sellable, removed only `testSellable`, `acceptanceCandidate`, and `acceptancePurpose`, and retained legitimate pricing/mapping metadata. Normal checkout and live payment capture remain false.
- Added a verified acceptance summary to `/orders`, preserved historical Public status reads after gate closure, and canonicalized known case-variant Admin paths so `/ORDERS` replaces to `/orders` and consistently renders `Admin / Orders`.
- Added the distinct Master-only pre-cutover Stripe TEST acceptance gate while normal checkout, live payment capture, and fulfillment remain disabled. The Admin action reuses the real D1/order/Stripe Checkout core and permits only `product-397267935` / `variant-5019554081` (Third Rail Farm black mug, 11 oz / Black, CAD 15.00), quantity one, with one deterministic local order/Session.
- Added safe exact-Session order status, a truthful Public processing/confirmed/not-confirmed success flow, and an upgraded `/orders` TEST view with immutable product/variant line, CAD total, Session state, payment state, disabled fulfillment, no Printful order, and safe hosted URL.
- Preserved signed webhook authority and idempotency. Payment confirmation cannot start fulfillment, send a fulfillment email, decrement inventory, mutate the independently paused Printful migration, call Printful, change Wix, enable live Stripe, or affect custom-domain DNS.
- Reused the existing bounded merchandising JSON plus first-class visibility/status/price fields for product images, descriptions, categories/tags, display order, and variant display labels; no commerce migration was required.
- Replaced the featured-only `/products` surface with searchable/filterable product and variant management, integer CAD editing, provider-readiness presentation, protected mutations, and commerce audit records.
- Added sanitized public catalogue and product-by-slug projections. Provider IDs, migration details, SKUs, and internal metadata are excluded.
- Added a deterministic 49-product merchandising reconciliation while leaving My Balloon private, sellability unchanged, checkout disabled, and the Printful checkpoint untouched.

CURRENT VER=0.0.0-seed

PENDING VER=0.1.0-alpha.0

No prior version metadata or release scheme existed in this repository. `0.0.0-seed` names the seeded pre-scaffold state; `0.1.0-alpha.0` establishes the first pending, non-production Admin milestone.

## Pending 0.1.0-alpha.0

### Technical

- Added the narrow Admin contact-delivery authority used by the Public homepage form. The endpoint accepts only the configured Public origin, bounded JSON and valid `thirdrailify-contact` Turnstile proof; verifies the Public relay's fresh HMAC-bound one-way client rate token before applying the existing D1-backed limiter; retains honeypot behavior; validates name, reply email, topic, message and privacy acknowledgement; and sends through the existing Resend credential to `info@thirdrailify.com`, copying `thirdmailify@gmail.com` and using the visitor email only as Reply-To. Direct callers cannot forge rate buckets, raw client IP is not form content, and the shared mail helper's backward-compatible optional CC/per-message Reply-To fields leave account and GOATS delivery unchanged. No secret, D1 schema, inbox record, newsletter, marketing flow, or Cloudflare resource was added.
- Added first-class Admin collection authority and Shop navigation: expandable Shop now contains Products, Collections, and the single Orders entry. Additive migration `0009_commerce_collections.sql` creates stable collection IDs/slugs and many-to-many assignments, deterministically backfills the seven existing category identities, and preserves all product/variant rows. The protected editor supports metadata, visibility, order, counts, searchable assignment from collection and product views, revision conflicts, safe archive, empty-only hard delete, and audit. Product thumbnails now use immutable first-party Commerce R2 media URLs. The former always-present featured save control is now a compact Save/Discard rail shown only after a genuine featured-order change.
- Added profile-only animated GIF support to the GOATS submission and moderation media authority. The strict parser validates the complete GIF block stream, preserves frame controls and standard looping, strips unrelated extensions, rejects GIF for main/gallery roles, and stores/serves the validated `image/gif` unchanged through the existing private R2 boundary. Migration `0008_goats_profile_gif.sql` narrowly extends the existing `community_media` constraint without changing records, relationships, retention, or storage architecture; Admin add/replace inputs expose GIF only for profile media, including adding a profile when none exists. Focused integration coverage proves two-frame retention, role rejection, D1 persistence, and existing approval/media projections.
- Applied migration `0007` only to the existing `thirdrailify-commerce` D1, uploaded the nineteen normalized role images only to the existing `thirdrailify-profile-media` R2 bucket, and imported nine Wix records idempotently. The exact retry wrote zero rows; remote authority now contains eleven approved GOATS (nine Wix plus two retained demos), nineteen media-role rows, global comment/reaction defaults `auto`/`auto`, and no public private-email/Wix-owner leakage. Deployed the coupled Admin editor/API artifact as `066337b5-eae2-4fde-8c98-f44dbb15f474` (`066337b5.thirdrailify-admin.pages.dev`).
- Imported the owner-supplied full Wix `WildGoats` CMS export as nine additional approved/published GOATS records without replacing the two existing synthetic demos. The rerunnable builder validates all nine rows and eighteen supplied source files, normalizes them into nineteen metadata-stripped WebP role records (the one-file Just Gina submission retains separate Wix main/profile roles), emits opaque R2 keys plus idempotent D1 inserts, preserves authentic legacy aggregate counts without inventing account reactions, and confines Wix email/owner/source metadata to the authenticated Admin record.
- Added `commerce-migrations/0007_goats_engagement_and_wix_import.sql`. Comments and reactions now have global `disabled` / `auto` / `moderated` defaults and per-listing `inherit` / `disabled` / `auto` / `moderated` overrides. Auto remains the default; moderated writes enter explicit pending queues and do not affect public totals until approval; disabled listings reject new writes while retaining existing approved totals.
- Approved GOATS are no longer frozen after publication. The Master Admin detail editor can update public story, product mapping, rating, identity/location/coordinates, interaction policy, media main selection/order, and can add, replace, or remove non-main images through private validated R2 ingestion. Dedicated comment/reaction queues support pending approval, hide, and restore, while a new defaults page controls inherited behavior.

- Implemented `/content` as the Master Admin Public-banner editor with separate Normal Promo and Automatic Live Now sections, loading/unavailable/validation/unsaved/saving/saved/failure states, bounded message/CTA fields, static/ticker/crossfade and speed controls, live label/title-display/supporting-copy/action/animation/intensity controls, a locked `/watch/live` destination, and a clearly labelled non-live fixture preview.
- Added `commerce-migrations/0006_site_banner.sql`, a dedicated singleton banner configuration table in the existing Admin-owned `thirdrailify-commerce` D1. Authenticated mutations require exact Admin origin, current Master session, CSRF, the existing D1-backed rate limiter, strict server normalization, optimistic revision matching, and an auth audit event. No auth table stores banner content and no Cloudflare resource or secret was added.
- Added cacheable GET-only `/api/banner` safe projection and protected `/api/admin/banner` read/write routes. Public fields contain only bounded presentation configuration; revision, actor, audit, persistence, session, and mutation capability stay private. Function and Playwright coverage proves malformed/oversized/unsafe links and unsupported effects fail closed, non-Master/CSRF/origin requests are rejected, valid saves persist and audit, fixture previews remain explicit, and the editor has no horizontal overflow at 1440, 1024, 768, and 390 pixels.
- Applied only `commerce-migrations/0006_site_banner.sql` to the existing remote `thirdrailify-commerce` D1 (`3dd23a7e-7c64-49cb-a52c-c1540b41db1c`), then deployed the fresh Admin artifact first to existing Pages production/staging deployment `81d6b9a3-8fba-47db-87cf-48e2b21e03a7` (`81d6b9a3.thirdrailify-admin.pages.dev`). The full Admin validation matrix passed under Node 22.16.0: lint, typecheck, all 136 Function tests, 3 Watch tests, 4 Admin UI tests, the responsive banner browser suite, production build, diff hygiene, and changed-file secret scanning. Stable and immutable origins returned the safe banner projection, the unauthenticated editor API failed closed with `401`, and the Public proxy matched the Admin projection exactly; authenticated editor save/refresh persistence was proven by the local server-backed browser and Function acceptance without exposing credentials or weakening remote login controls.

- Added a compact direct-source action to every retained Watch row. Each button uses the authority-supplied validated `watchUrl`, opens Rumble or YouTube directly, and displays the matching local provider SVG; it remains available when an episode is hidden while the existing Public Preview correctly remains visible-only. Responsive browser fixtures now cover populated visible/hidden rows and both platforms at 1440, 1024, 768, and 390 pixels.
- Refined the Master Admin Watch workspace into a compact operational control room without changing its security or write path: a concise `Watch / Broadcast` header, Public Watch action, five-part retained/visible/hidden/unfilled/current-signal strip, current title/platform/timing panel, and denser thumbnail/list/action rows replace the oversized marketing-style composition.
- Preserved fail-soft authority semantics, em-dash unknown values, per-record mutation focus restoration, disabled empty-state controls, and mandatory focus-restoring bulk-hide confirmation. Responsive coverage now explicitly includes 1440×900, 1024×768, 768×1024, and 390×844 without false zeros, horizontal overflow, or phone column squeeze.
- Corrected both Watch browser harnesses to serve current source instead of potentially stale `dist`; the Admin heading/capacity assertions now verify the refined UI without weakening authority expectations. Validated under Node 22.16.0 with lint, typecheck, Watch tests, 110-test full Functions retry, Admin UI tests, Watch browser tests, production build, diff hygiene, and changed-file secret scanning. The first full Functions attempt had one transient SQL.js dependency `fetch failed`; its complete file passed 6/6 immediately and the exact full command then passed 110/110.
- No Admin-to-Public HMAC contract, session/origin/CSRF/rate-limit/audit enforcement, archive mutation, shared secret, bot environment, commerce/email work, or Cloudflare resource changed.
- Deployed only the existing Admin Pages project as deployment `35cb8ac0-ba3d-49f7-8672-68f42a185582` (`35cb8ac0.thirdrailify-admin.pages.dev`). Stable-origin headless acceptance reached the configured application sign-in at `200` with no overflow or page-origin console errors, but Turnstile intentionally did not issue an automated token, so no authenticated remote action or visibility mutation was attempted; authenticated authority rendering remained covered by the passing fixture browser suite at all four required widths.
- Fixed the deployed Admin Watch outage after production tracing isolated the upstream response as `401 invalid_signature`: Public Watch remained healthy because its visible APIs require no management signature, while Admin's hidden-inclusive authority read requires matching encrypted `THIRDRAILIFY_COMMUNITY_API_SECRET` bindings. The owner restored one matching value in both Pages projects; no secret entered source, logs, responses, or browser code.
- Admin now signs the protected bodyless `GET /api/watch/manage` read with the exact method/path/empty-body digest and uses a normal abort-bounded Cloudflare Pages subrequest without a synthetic `Origin` or incompatible redirect option. JSON `POST` mutation signing, Master session, exact origin, CSRF, rate limiting, audit, and visibility controls remain unchanged. Authority failures render unknown values instead of fabricated zero counters, while a legitimately absent current snapshot remains distinct from an unavailable archive.
- Deployed the fresh isolated Watch-specific Admin artifact after secret synchronization as Pages deployment `92753eb4` (`92753eb4.thirdrailify-admin.pages.dev`). Authenticated Playwright MCP acceptance on the stable URL returned `200`, removed the unavailable warning, and rendered the live authority as retained 2, visible 2, hidden 0, unfilled 22, including `hey you` and the newer retained Rumble episode.
- Activated the existing opt-in GOATS seed on staging with exactly `DEMO-GOAT-01` Midnight Rail and `DEMO-GOAT-02` Southern Signal. The records are synthetic, media-free, coarsely located, idempotent, outside migrations/startup, and still removable by the existing cleanup script, which was not run.
- Added a first-class Master-only `/watch` workspace for current broadcast posture and the retained Public archive: retained/visible/hidden/unfilled counts, newest/oldest context, branded fallbacks, stable identity/date/order/status, show/hide one, show/hide all, bulk-hide confirmation, focus restoration, public preview, and truthful loading/empty/unavailable/save states.
- Added `POST /api/admin/watch`, which enforces the existing Master session, exact Admin origin, CSRF, bounded action/episode ID, `watch` rate limit, and audit path before signing the fixed Public management endpoint with the already-configured encrypted `THIRDRAILIFY_COMMUNITY_API_SECRET`. The browser never receives the secret, Admin has no Durable Object binding, and no new secret or Cloudflare resource is required.
- Documented the signed Admin-to-Public boundary, natural no-scrape signed-ingest archive population, 24-record retention including hidden records, placeholder semantics, and the legitimate empty-to-real archive transition in `docs/WATCH_V2.md`; no synthetic Watch history was inserted.
- Built the Admin Watch artifact from an isolated HEAD-plus-Watch tree so concurrently appearing unrelated Printful/email work was preserved locally but excluded from this deployment. The Watch artifact deployed as `a0463117-ce34-423b-a3d8-0ffe636eac8e` (`a0463117.thirdrailify-admin.pages.dev`); subsequent concurrent Admin deployments also contain the Watch route. No custom domain or production cutover was changed.

- Fixed the separate live Printful V1 429 acceptance failure that appeared after the Pages subrequest fix. All source and target catalogue GETs now share one 675 ms request-start scheduler (about 88.9/minute), with the signed `nextProviderRequestAt` envelope surviving Pages calls; in-flight work is sequential and deterministic rather than a four-request burst. Safe `Retry-After` and `X-RateLimit-*`/`X-Ratelimit-*` timing values are consumed without exposing unrelated headers.
- Made 419 and 429 recoverable snapshot states. Pagination, source/target product chunks, and source/target file chunks retain normalized successes plus the exact first-uncompleted cursor in signed checkpoints; Retry-After or a later reset is honored with a safety margin, and missing timing uses a 62-second fallback. The browser shows phase/completed/total/provider state and an absolute-time countdown, then automatically resumes without another click or re-reading completed items. Twelve provider throttle cycles are allowed before a safe terminal result retains progress.
- Added a dedicated bounded continuation rate budget while keeping snapshot starts on the existing commerce budget; every continuation still requires Admin authentication, exact origin, CSRF, Master or `commerce.integrations.manage`, commerce D1, and valid signed evidence. No D1 migration or catalogue persistence was added.
- Corrected the remaining GOATS email typography mismatch: the sandboxed recipient preview and actual Resend HTML now use the same existing `American Captain` headings, `Blinker` body copy, `Geist Mono` labels/actions, and zap mark as the Customer Emails preview. Stable, cacheable Admin-hosted font assets are referenced by delivered HTML with email-safe fallbacks for clients that block web fonts; the independent plain-text fallback is unchanged. Deployed from clean baseline `7000561` as Pages deployment `2cfc1681-e1ce-4b93-9849-ea9c51440a76`; all three canonical remote font loads passed browser `document.fonts` checks, Admin/Public GOATS remained `200`, and protected templates remained unauthenticated at `401`.
- Promoted GOATS from a single sidebar link to a first-class expandable navigation group matching Commerce, with distinct icons and direct routes for overview, pending, approved/published, rejected, comment moderation, and lifecycle emails. Active GOATS routes auto-expand the group while collapsed-desktop and mobile navigation behavior remains shared.
- Rebuilt the GOATS email workspace to the Commerce-grade template/editor pattern with readable template names, revision/state visibility, documented variable chips, bounded HTML and plain-text fields, safe branded recipient preview, and an explicit text-only preview. Actual Resend delivery now wraps the existing Admin-authored content in the same responsive Third Railify/GOATS HTML shell without changing template status, outbox idempotency, escaping, permissions, CSRF, or save-without-send behavior.
- Deployed the isolated GOATS Admin update from baseline `d5f4811` as Pages deployment `c55e6a6c-e037-4762-997c-2c21bad52d9c`. The canonical Admin GOATS route and immutable deployment both returned `200`, the deployed bundle contained all GOATS child navigation and branded-preview labels, unauthenticated email-template access remained `401`, and Public/Admin approved-listing projections both returned the same legitimate empty result.
- Fixed the failed live Printful catalogue operator acceptance. The earlier one-request implementation needed at least 54 external provider calls for the known 49-product Wix source plus one API-target product (two identity reads, two list reads, and 50 detail reads), exceeding the 50-external-subrequest Pages/Workers Free ceiling; the runtime exception was incorrectly collapsed to the permanent generic `Printful catalogue discovery is temporarily unavailable.` banner.
- Kept `POST /api/admin/commerce/printful/catalogue/snapshot` and changed one operator click into same-route signed phases: verified/paginated manifest, bounded 12-product detail chunks, bounded conditional file-metadata chunks, and server-verified final assembly. Each provider invocation remains below 50 with bounded transient retries; all detail starts now use the shared paced scheduler and final ordering is deterministic.
- Decoupled action visibility from prior snapshot state, product/order counts, checkout, fulfillment, and the legacy secret-presence projection. Exact safe IDs `16847493` and `18668025`, unequal stores, commerce audit storage, the permanent integration, and Master or `commerce.integrations.manage` expose the action; a missing legacy token fails closed only after the protected action begins.
- Replaced four unsolicited automatic downloads with ready/running/completed/failed states, safe phase progress, actionable provider role/category errors, retry, result counts, and four explicit controls for `printful-wix-source.snapshot.json`, `printful-api-target.snapshot.json`, `public-wix-catalog.snapshot.json`, and `catalogue-reconciliation.json`. No live-runtime filesystem dependency exists, and no authenticated live snapshot is claimed before the operator runs it.
- Recovered the interrupted catalogue milestone from commit `2eae387`, bound safe `PRINTFUL_WIX_SOURCE_STORE_ID=16847493`, and hardened the source/target invariant to exact token-resolved/configured identities: legacy `Third Railify Official` / `16847493` / `wix`, permanent `Third Railify API` / `18668025` / `native`, and unequal IDs.
- Completed the permission-gated read-only catalogue action with full pagination, every product-detail read, conditional file-metadata reads, exact decimal-to-CAD-cent parsing, deterministic safe normalization, stable-identifier-first reconciliation against the eight-product Public Wix projection, bounded audit counts, and four browser-downloaded sanitized artifacts. All Printful transport remains hard-coded to GET; commerce products/orders and checkout/fulfillment gates are untouched.
- Added the design-only next variant schema at `commerce-import/0005_commerce_product_variants.proposed.sql` because applied migration `0004_goats_community.sql` already owns number 0004. The proposal is not in the migration directory and is not applied.
- Validated with Node 22.16.0 lint, typecheck, production build, 82 passing Admin Functions tests, focused catalogue tests, migration regressions, Pages Functions compile, and fixture-backed 390/768/1440 rendered checks with one H1, no overflow, no console errors, and no credential text. Deployed Admin Production/main once at `80c8ceb6.thirdrailify-admin.pages.dev`; Public was not deployed and the live catalogue action still requires the legitimate authenticated operator click.
- Added the permanent store-scoped Printful verification seam without a D1 migration: `POST /api/admin/commerce/printful/verify` reuses exact Admin origin/session, Master or `commerce.integrations.manage`, CSRF, the bounded commerce rate limit, and existing auth/commerce audits. The server accepts an opaque bounded `PRINTFUL_API_TOKEN`, performs only `GET /stores` and `GET /store/products?limit=1`, sends no discovery store header, and exposes no credential material.
- Made Wix-store isolation permanent: verification requires exactly one whitespace/case-normalized `Third Railify API` store of Printful's `native` type, rejects Wix/multi-store/ambiguous identities before the product probe, and fails closed when configured, token-resolved, and persisted Store IDs disagree. Only the existing unique Printful provider row receives safe Store ID/name/type/count proof; no token, Authorization header, raw response, product, order, webhook, or duplicate provider row is stored.
- Upgraded Commerce Overview and Fulfillment Integrations to show the real production-capable Printful API separately from the pre-cutover application rollout, including safe store identity, single-store access, product count, `draft_only`, disabled automatic fulfillment, unconfigured webhooks, and explicit confirmation that the active Wix integration remains unaffected. Added a permission-gated verification control with no browser credential path.
- Added `tests/printful.test.mjs` and `npm run test:printful` for opaque/bounded credential handling, auth/origin/CSRF/capability gates, existing commerce rate limiting, exact GET/header behavior, malformed/zero/multi/Wix/ambiguous rejection, empty and non-empty product probes, safe persistence/audit, no duplicate provider or commerce records, unchanged checkout/live/fulfillment gates, no provider write method, and permanent three-way Store-ID matching.
- Verified the deployed Admin seam against the permanent store-scoped token: it resolved exactly one `native` store named `Third Railify API`, safe Store ID `18668025`, and one visible product. Bound only that safe ID as `PRINTFUL_STORE_ID`; the Wix store was not selected or accessed, and no Printful write was performed.

- Added additive `commerce-migrations/0004_goats_community.sql` as the Admin-only authority for drafts/submissions, opaque media metadata, reactions, plain-text comments, moderation events, idempotent email outbox events, templates, and privacy-hashed rate limits. It inserts four templates with `INSERT OR IGNORE`, no production listing, and leaves existing migrations unchanged.
- Added the first-class Master Admin GOATS overview, Pending/Approved/Rejected queues, private moderation detail, comment moderation, and email-template workspace. Moderators can correct public fields/coarse coordinates, order/select media, approve/reject, hide/restore, preview, retry delivery, and delete only fixed `DEMO-*` records with optimistic versions; approval fails without ready main media, valid product/slug/consent/fields, and confirmed coordinates.
- Added the fixed approved-only `/api/goats/*` projection/ingestion authority and protected `/api/admin/goats/*` surface. Public-to-Admin mutations require a five-minute HMAC signature; Admin writes retain exact origin, current server-resolved Master role, CSRF, parameterised D1, and no-store output. Status change, audit event, and idempotent status-email event are one D1 batch.
- Reused the existing Admin-only `THIRDRAILIFY_PROFILE_MEDIA` R2 binding under opaque `goats/private/...` keys. Server validation caps files at 10 MiB, verifies declared MIME plus JPEG/PNG/WebP structure, strips EXIF/GPS/XMP/unnecessary metadata, constrains dimensions, and exposes only ready media belonging to approved/published records. Expired drafts have an explicit bounded Master maintenance cleanup path.
- Added received/Admin-alert/approved/rejected branded HTML and plain-text templates, escaped variables, safe sandboxed fixture previews, header-injection rejection, fake-transport tests, background ready-template dispatch, visible retry state, and provider-idempotency keys. No real email was sent.
- Added the owner-export Wix JSON contract and dry-run validator. Exactly two invented `DEMO-*` listings exist only in an opt-in, idempotent local/test seed with exact cleanup; they are not migrations, production data, or automatic startup state. Added focused migration/core/media/privacy/reaction/comment/email/state tests and documented all unapplied secret/binding/provider prerequisites.

- Revised `commerce-migrations/0003_product_merchandising.sql` before its first remote use: it preserves featured state/order but removes all legacy Wix snapshot inserts, so the authoritative catalogue remains empty. It adds bounded server price/environment/visibility/quantity/shipping fields, idempotent Checkout/order linkage columns, immutable normalized order-item snapshots, and a webhook-ledger status extension that copies the historical `accepted_noop / checkout_disabled` row unchanged. Applied only this migration to the confirmed commerce D1 and verified no migrations remain, with zero products, orders, and order items and the historical receipt unchanged.
- Implemented exact-Public-origin `POST /api/commerce/checkout` with narrow CORS/OPTIONS, 16 KiB body limits, 20-line/20-quantity bounds, UUID request idempotency, optional setting-gated Turnstile, and the existing hashed D1 rate limit. Browser monetary/name/currency/tax/shipping/discount/Stripe-ID fields are rejected; only product IDs and quantities are accepted.
- Added server-only active/public/test/CAD product validation and integer-minor-unit totals from `commerce_products`. A local `checkout_pending` order plus immutable line snapshots exists before the direct-merchant `POST /v1/checkout/sessions`; Stripe receives inline server-derived `price_data`, card-hosted Checkout semantics, allowlisted Public success/cancel URLs, safe order correlation metadata, and a deterministic idempotency key without `Stripe-Account`.
- Upgraded the signed webhook receiver so a linked `checkout.session.completed` can move one existing TEST order from `pending` to `paid` exactly once only after order/client reference, Session ID, checkout request, mode, CAD total, paid status, and environment all agree. Unknown/mismatched/unpaid/live events remain bounded ledger no-ops, duplicates cannot add a row or repeat the transition, and fulfillment/Printful/email/inventory/VIP actions remain absent.
- Replaced the deferred Orders shell with a read-only Admin order-state surface and updated Commerce/Payments status to distinguish the implemented gated engine from disabled Public checkout, zero authoritative products/orders, TEST payments, and disabled fulfillment. Added focused Checkout/order/webhook/migration coverage with mocked Stripe and synthetic D1 only.
- Validated the milestone with Node 22.16.0 lint, typecheck, production build, Pages Functions compile, all 49 Admin Functions tests, fresh 0001/0002/0003 migration regression, and authenticated fixture-backed rendered checks at 390, 768, and 1440 pixels. Deployed only Admin Production/main as `1a9b7e5b-33fe-4bc5-b186-1f24c7e7ecca`; the canonical gated probe returned `409 checkout_disabled`, and remote counts remained zero products/orders/order items with one unchanged webhook receipt and all activation gates false.
- Replaced the deferred `/products` shell with a real accessible merchandising workspace: Master-only feature toggles, stable move-up/down order, hero preview, missing-image/price warnings, and a single save action backed by the existing Admin session, exact origin, CSRF, D1 rate limit, transactional batch, and redacted commerce/auth audits.
- Added authenticated product-management GET/POST routes plus a separate cacheable GET-only `/api/catalogue/merchandising` projection exposing only product ID, slug, featured state, and numeric order. Public receives no commerce binding, metadata, price, image, credential, account, or write authority; duplicate/unknown/non-displayable product submissions fail closed.
- Added earlier focused migration and Function coverage for product identity fixtures, featured constraints/order, Master authority, CSRF path, persistence, audit, projection field exclusion, and deterministic public ordering. The current Checkout milestone removes those fixture identities from the pending remote migration; synthetic products now exist only in isolated tests.
- Validated with Node 22.16.0 lint/typecheck, 38 auth/profile/commerce/webhook tests, and production build. Fixture-backed authenticated Edge checks exercised eight products, four featured entries, keyboard-accessible reordering, the protected save flow, one page heading, and zero horizontal overflow or console errors at 1440×900, 768×1024, and 390×844.
- Reconciled Stripe configuration proof semantics without a schema migration: successful CA/CAD `GET /v1/account` verification now atomically updates provider `api_configured` plus canonical `stripe_api_configured`, preserves earlier webhook proof, and never sets the flag from secret presence or a failed verification.
- Valid signed, timely, well-formed sandbox Stripe Events now atomically persist/reuse the safe receipt and set canonical/provider webhook configuration proof. Valid duplicates reassert proof without another ledger row; unsigned, invalid, stale, malformed, or live-mode deliveries cannot set it, and Checkout/live-payment/fulfillment gates remain false.
- Updated `/commerce` and `/commerce/payments` to distinguish connected test API, operational/verified webhook signing, disabled Checkout/live payments/fulfillment, and unverified live payout readiness without exposing secret or event material.
- Reconciled only the two stale safe setting rows and Stripe provider webhook proof in the confirmed commerce D1 after exact provider/event/order invariant checks, recorded one bounded `stripe.configuration_reconciled` audit event, and preserved the single accepted-noop receipt with zero orders. Deployed Admin Production/main as `f69ea81a-5c6e-4c38-8389-3604bfd135c4`; no Stripe API/event operation or Public deployment was performed.
- Added a live, structured rendered preview below every Customer Emails template editor. Subject, preheader, heading, introduction, bounded body blocks, CTA, support copy, footer, status, and accent update without raw HTML execution or sending, while the email brand header now prefixes `THIRD RAILIFY OFFICIAL` with the tracked `trzapcolorcon.svg` icon.
- Added the public machine-to-machine `POST /api/webhooks/stripe` Pages Function without browser auth, CSRF, Turnstile, Origin, CORS, or commerce capability requirements. It instead streams at most 1 MiB of exact request bytes, requires the server-only `STRIPE_WEBHOOK_SECRET`, verifies any valid Stripe `v1` HMAC-SHA256 signature with Web Crypto, and enforces a fixed 300-second past/future timestamp window before JSON parsing.
- Added `commerce-migrations/0002_stripe_webhook_events.sql` with a composite `provider + provider_event_id` primary key and bounded safe receipt metadata/indexes. Signed Sandbox `checkout.session.completed` deliveries are stored only as `accepted_noop / checkout_disabled`; unknown valid test events are acknowledged as ignored, duplicates are successful no-ops, and raw bodies, signatures, secrets, credentials, customer/card/address data, and full Stripe objects are excluded.
- Split the Stripe Admin projection into truthful webhook endpoint and signing states. The endpoint is ready for Workbench configuration, signing remains not configured until `STRIPE_WEBHOOK_SECRET` exists, and Checkout/live payment/fulfillment gates remain false. Added synthetic signature, raw-body, replay, envelope, livemode, no-mutation, idempotency, sensitive-data exclusion, migration-order, and existing-verification regression coverage.
- Documented the exact next Stripe Sandbox Workbench event destination: Your account, Snapshot event, normal/default supported API version, only `checkout.session.completed`, and `https://thirdrailify-admin.pages.dev/api/webhooks/stripe`, followed by Admin Production encrypted-secret custody for the generated `whsec_...` value. No Stripe destination or secret is created by this milestone.
- Applied only `0002_stripe_webhook_events.sql` to the confirmed `thirdrailify-commerce` D1 (`3dd23a7e-7c64-49cb-a52c-c1540b41db1c`), verified no migrations remain and the empty ledger exists, then deployed Admin Production/main as `c7fb003a-5bc4-4872-8ee8-30f6fbc15032`. The canonical unsigned route returned `503 stripe_webhook_not_configured`, proving the receiver is live and fails closed before signing-secret provisioning.
- Corrected the Firefox-only sign-in scrollbar regression caused by an unreliable WebKit-selector support probe: Firefox now explicitly retains its original narrow gold-on-dark `thin` scrollbar, while Chromium/Brave keeps the separate five-pixel treatment.
- Added the authenticated `POST /api/admin/commerce/stripe/verify` action and `/commerce/payments` control for one read-only direct-merchant `GET https://api.stripe.com/v1/account`. It reuses Admin origin/session, `commerce.payments.manage`/Master authority, CSRF, the bounded commerce rate limit, and redacted auth/commerce audit models.
- Made staging Stripe server credential validation environment-aware under the unchanged `STRIPE_SECRET_KEY` name: intended restricted `rk_test_...` and compatible `sk_test_...` credentials are accepted, while `rk_live_...`, `sk_live_...`, missing/malformed credentials, missing D1, and non-CA/non-CAD accounts fail closed.
- Persisted only the verified existing Stripe provider row's safe account ID, Canada/CAD identity, display name, account type, test charges/payouts/details flags, and verification timestamp. Test API connection uses existing `connected` status without enabling webhook, Checkout, live payments, or live payout readiness; secrets and raw Stripe payloads remain excluded.
- Aligned the Admin sign-in provider gallery with the existing provider-state contract used by Public so disabled Google remains visibly staged with migration-status copy while unavailable providers stay hidden and no OAuth gate or credential is enabled.
- Made the shared sign-in dialog reliably viewport-bounded in Chromium/Brave as well as Firefox by removing grid-item shrink ambiguity, using dynamic-viewport height with a compatible fallback, and containing vertical scrolling inside the modal so verification and submit actions remain reachable through a minimal five-pixel Chromium scrollbar or Firefox's narrowest supported `thin` treatment.
- Grouped the five existing `/commerce/*` workspaces beneath an accessible expandable Commerce Overview sidebar branch while preserving every direct route, replaced reused generic symbols with purpose-specific commerce/payment/business/tax/email/fulfillment icons, and upgraded the overview with an always-available responsive destination-card gallery.
- Hid the Admin shell sidebar scrollbar across Firefox and WebKit/Chromium while preserving its existing independent vertical scrolling, navigation structure, and desktop/mobile behavior.
- Bound the confirmed Cloudflare `thirdrailify-commerce` D1 resource (`3dd23a7e-7c64-49cb-a52c-c1540b41db1c`) only to Admin as `THIRDRAILIFY_COMMERCE_DB`; commerce migrations have not been applied, and Public has no commerce D1 binding.
- Applied the existing `commerce-migrations/0001_commerce_control_plane.sql` migration to the confirmed Cloudflare `thirdrailify-commerce` D1 (`3dd23a7e-7c64-49cb-a52c-c1540b41db1c`); no production commerce data or provider credentials were seeded.
- Corrected the unprovisioned commerce scaffold from an obsolete Stripe Connect platform/connected-account model to the dedicated Third Railify Official Canadian merchant account. Stripe is now a single `stripe` provider with canonical `direct_merchant` mode, `environment_secret` custody, operator-confirmed account creation, Canada/CAD, and truthful setup-required API/webhook/checkout/live-readiness flags.
- Replaced the two Connect-specific migration seeds and onboarding setting in place because no commerce D1 has been provisioned. Added a constrained generic integration-mode field, rejected legacy Stripe provider identifiers, preserved all commerce permissions/entities, and added the missing separate Printful refund/credit accounting field.
- Updated Admin commerce overview, Payments & Payouts, Fulfillment, navigation, architecture, feasibility, Cloudflare runbook, README, and focused tests. The dedicated account does not imply API connectivity, live readiness, payout readiness, wallet availability, or enabled checkout; no Stripe/Printful/PayPal request or remote mutation was made.
- Established the Admin-only Stripe-first Canadian commerce control-plane scaffold without activating commerce: added truthful `/commerce` overview, Payments & Payouts, Business Information, Tax & Documents, Customer Emails, Fulfillment Integrations, and upgraded Products/Orders surfaces. Checkout, live capture, onboarding, provider connection, and fulfillment submission remain disabled.
- Added the idempotent local `commerce-migrations/0001_commerce_control_plane.sql` authority for a future separate `thirdrailify-commerce` D1. It models business/tax profiles, constrained provider custody/status, structured templates, settings, capability grants, provider-neutral products, separately accounted Stripe/Printful order economics, and redacted audit history; no D1 was created, bound, or migrated remotely.
- Added a server-only AES-256-GCM envelope for encrypted private business/legal/tax and future `admin_encrypted` credentials, with a 32-byte base64url key, random nonce, authenticated purpose, bounded plaintext, versioning, tamper/wrong-key rejection, and no plaintext fallback. Stripe and Printful tokens remain Cloudflare Secrets.
- Reused existing auth/session/role/origin/CSRF/rate-limit/audit authority for five commerce capabilities. Master Admins have all capability authority and alone can grant/revoke it; Full Admins can view and receive bounded grants; ordinary users are rejected.
- Recorded a fresh non-mutating Wix public audit, the existing bounded Public snapshot, owner-confirmed private Wix provider facts, and unresolved dashboard-only facts. Wix remains production authority and no Public/Wix/provider state was changed.
- Historical and superseded before provisioning: documented a Canadian Connect account design with hosted onboarding and platform-specific eligibility checks. The dedicated direct merchant account correction above is now authoritative.
- Documented the separate customer-payment and Printful billing transactions, parallel manual/API Printful store, environment-managed store token, safe Store ID, draft-only orders, and explicit no-confirm posture. PayPal remains a later direct-merchant donations/VIP integration; Printify remains unavailable pending evidence.
- Added focused commerce migration, constraints, crypto, fail-closed storage, projection/masking, permission, CSRF, audit-redaction, template-safety, provider-truth, and two-transaction tests. No provider APIs or live resources are used.
- Corrected email sign-in so the browser no longer applies the 12-character new-password policy to existing credentials; signup and password reset retain the 12-character minimum. Added CSRF-protected, rate-limited, audited self-service display-name updates through the Admin D1 authority, including durable custom names for environment Master accounts.
- Moved the oversized sidebar collapse control into the desktop top bar as a discreet icon-only action pinned 12px from the sidebar/workspace boundary before the breadcrumb; mobile continues using only its established drawer button.
- Restored the Admin account trigger from an unrequested pill treatment to the shell's existing 9px squared control radius without changing its identity content or dropdown.
- Upgraded the Admin shell with a persistent desktop icon-only sidebar state, full-width mobile drawer behavior, and a StreamSuites-reference account menu adapted to Third Railify with an identity header, details matrix, icon-prefixed actions, keyboard navigation, and separated sign-out treatment.
- Added Admin-authoritative avatar changes to `/access`: JPG/PNG/WebP uploads and public HTTPS URL imports are session/CSRF/rate-limit protected, capped at 5 MB, byte-validated, stored as immutable `u/<opaque-account-key>/avatar/<sha256>.<ext>` objects through the Admin-only `THIRDRAILIFY_PROFILE_MEDIA` R2 binding, and persisted to D1 only as a clean HTTPS URL. The binding/custom domain remain manual deployment prerequisites.
- Added a shell-level horizontal loading indicator adapted from the StreamSuites Dashboard/Public pattern with Third Railify gold gradients, real Overview/Accounts async tracking, concurrent-load-safe tokens, main-content `aria-busy` state, and a static reduced-motion treatment.
- Switched the Admin dashboard favicon to the supplied `assets/icons/thirdadminfavx.png` artwork with an explicit PNG MIME declaration; all previous icon assets remain preserved but inactive.
- Wired the confirmed production Google OAuth client ID while keeping staging explicitly gated with `GOOGLE_OAUTH_ENABLED=false`. Admin now exposes truthful provider states and blocks both Google OAuth start and callback/token exchange until the gate is enabled with both credentials present; the production callback remains deferred to the domain migration and derived from `THIRDRAILIFY_ADMIN_ORIGIN`.
- Established `ThirdRailify-Admin` as the shared D1 account authority with an idempotent schema for accounts, identities, PBKDF2 credentials, hashed sessions, OAuth transactions, public handoffs, verification/reset tokens, rate limits, and bounded audit events.
- Added explicit Turnstile validation, Resend verification/reset flows, two locked environment Master Admins, Discord/Google/GitHub/X OAuth with one-time state and PKCE where supported, provider-subject uniqueness, verified-email linking, and immediate token discard.
- Added fail-closed Admin session/role gates, the real account widget, protected status hydration, and a functional `/access` registry. Full Admins may read; only Master Admins may promote, demote, enable/disable, or revoke sessions.
- Added exact auth/Admin Function routing, minimal Turnstile/avatar CSP allowances, safe staging origins, sanitized environment examples, and a complete manual Cloudflare/provider setup runbook. The missing real D1 ID remains an explicit activation blocker.
- Added focused D1 migration and auth integration tests. No live OAuth, Turnstile, Resend, Cloudflare resource, binding, secret, deployment, DNS, Access, or custom-domain change was performed.
- Added a Vite/React/TypeScript/React Router application with deterministic npm metadata.
- Added a responsive Third Railify operational shell, centralized route configuration, Overview, eight future-area shells, mobile navigation with Escape handling, visible focus, reduced motion, state examples, and a branded 404.
- Established the seeded American Captain face as the main display/header font, supported by Blinker and Geist Mono.
- Relaxed American Captain heading and brand-lockup tracking/line-height and removed faux-heavy rendering so titles remain condensed without crowding.
- Made system boundaries explicit: no auth, APIs, users, orders, memberships, analytics, provider state, privileged data, or writes are implemented or simulated.
- Added document/response noindex, restrictive static headers, SPA fallback, `.node-version`, and Cloudflare Pages staging documentation.
- Added lint, TypeScript, and production-build scripts. Validation evidence belongs in the completion report for this milestone.
- No Pages project, deployment, DNS, domain, provider, or Wix change was performed.
- Polished the document and scrollable sidebar with a narrow graphite/gold scrollbar treatment adapted from the approved POC.
- Rebuilt the sidebar lockup around the exact `assets/logos/boltv2.svg` silhouette, a POC-derived dark square, gradient-gold mask paint, and a larger `THIRD RAILIFY` title with a subordinate `CONTROL ROOM` label.
- Replaced the temporary inline favicon with the exact `assets/icons/boltv2.ico` asset.
- Checked the visual-polish milestone responsively at 390px, 768px, and 1440px, including the mobile sidebar, overflow, built assets, and browser-console behavior.
- Switched the Admin sidebar lockup to its exact local `assets/logos/boltv2straight.svg` copy while preserving the established gold-gradient mask, rounded dark square, inset highlight, and responsive lockup treatment.
- Replaced the active Admin favicon with the byte-identical local `assets/icons/thirdfavstraight.ico` copy sourced from the authoritative Public asset; previous ICO assets remain preserved but inactive.
- Validated the milestone with Admin lint, TypeScript, production build, diff checks, exact built-favicon bytes/content type, and rendered 390px/768px/1440px checks including the mobile sidebar with no overflow, broken assets, or console errors.
- Configured the confirmed safe Resend sender identity and reply-to address in Admin Wrangler without adding or changing the encrypted `RESEND_API_KEY`; no deployment or email acceptance test was performed.
- Wired the existing Third Railify Official Discord application ID (`1283057181578625034`) into Admin Wrangler as safe staging OAuth configuration.
- Wired the GitHub OAuth App client ID into safe Admin configuration. Its staging and production callbacks remain registered externally on the same GitHub App; GitHub stays credential-driven and becomes available only after the encrypted client secret is provisioned.
- Wired the confirmed X OAuth 2.0 Client ID into safe Admin configuration. The X application is configured externally as a confidential Web App with both staging and production callbacks; its separate numeric X App ID is not used by website authentication, and X becomes operational once `X_OAUTH_CLIENT_SECRET` is provisioned as an encrypted Admin secret.

### Human-readable

The new website now proves that its permanent Printful credential belongs only to the separate Third Railify API store (Store ID `18668025`). The check found one visible product, but it cannot create or change products, orders, files, or webhooks. Draft-only mode and automatic fulfillment remain firmly off, and the existing Wix store stays live and untouched.

The control room now has a complete local GOATS moderation desk. A Master Admin can review private submissions, make the public location deliberately approximate, verify the product and photos, publish or reject the story, hide it later, moderate comments, and see whether each status email is pending, failed, or sent.

Nothing has been placed into production: the new database migration is only checked in, the live gallery has not been populated, no email was sent, and the two invented proof listings run only when someone deliberately applies the local test seed. The later Wix export can be checked safely before an importer is added around its real shape.

The sandbox Checkout/order engine now exists behind the shut gate: it prices only from authoritative Admin D1 products, snapshots an order before Stripe, and trusts only the signed webhook to confirm payment. The remote catalogue remains empty, Public checkout remains disabled, and no payment or fulfillment was activated.

When authoritative products are imported later, the control room can choose which products headline the public shop and arrange their order with clear, keyboard-friendly controls. The public site receives only the tiny safe ordering projection and continues gracefully if Admin is unavailable.

The control room now remembers what Stripe has actually proved: the Canadian CAD test API is connected and a real signed sandbox event verified the webhook path. Those configuration checks remain separate from taking payments or fulfilling orders, which are still disabled.

Historical prior state: the first deployed webhook path could create only one safe receipt. The current engine preserves that receipt unchanged while allowing a future signed, fully matched TEST Session to confirm one already-created local order; it still cannot create an order from a webhook or fulfill anything.

Third Railify now has its own dedicated Canadian Stripe account, and the control room says exactly that. It also stays honest about what is still missing: webhook signing, Checkout acceptance, live verification, and payout readiness.

The control room now has an honest, reviewable commerce foundation for a future Canadian Third Railify store. It explains who owns the merchant account and payouts, keeps Shawn's Stripe identity and bank entry inside Stripe, separates customer income from Printful costs, and provides safe business, tax, and customer-template editors without pretending that checkout or fulfillment is live.

The current Wix shop remains completely in charge. The new Admin pages show exactly what is ready, missing, deferred, or unavailable, and they refuse private persistence when the separate commerce database or encryption key is absent.

Existing Master credentials can now sign in without being blocked by a password rule intended only for new passwords. Signed-in accounts can also change their display name alongside their avatar, and Master names remain customized after refresh.

The control room sidebar can now tuck down to its icons without losing navigation, while phones continue to receive a full readable drawer. The account menu now carries the same level of identity detail and action polish as the supplied StreamSuites examples, in Third Railify’s own black-and-gold treatment.

Admins can change their own avatar from Accounts & Access using a file or image URL. Uploaded bytes live behind a clean immutable media path instead of being stuffed into account URLs as browser data.

The Admin shell now shows a slim animated gold signal beneath the top bar while real dashboard data or account actions are loading, with a non-moving alternative for reduced-motion users.

The Admin surface now has a real shared account foundation and refuses to show the control room until the server confirms an Admin role. Master Admins can manage account access from the new Accounts page; every other operational module remains accurately deferred.

The separate Admin repository now has a real, polished frontend that can be reviewed on an isolated staging URL. It clearly shows where future operational areas will live while refusing to pretend that accounts, orders, products, or integrations are connected. It is a visual and routing foundation, not an administration system.

Admin now carries the confirmed public sender identity and reply-to address as safe configuration; the Resend API key remains remote and encrypted.

### Known deferrals

- Historical blocker removed: the superseded Stripe Connect platform eligibility check is no longer part of the Third Railify shop architecture.
- Authoritative product/variant catalogue import, Public cart/checkout client wiring, enabled sandbox Checkout acceptance, wallet eligibility testing, refund/dispute handling, and all live payment activity.
- Authoritative Printful catalogue reconciliation/import, draft-order API work, explicit order-confirmation gates, and fulfillment activation. The separate API store and scoped-token verification seam now exist; the Wix-connected Printful store remains untouched.
- PayPal direct-merchant setup and donations/VIP work; Printify evidence and credential custody; authoritative Wix catalogue/provider/export/policy reconciliation and cutover planning.
- Creation/binding of the real staging D1 database, remaining encrypted secrets, provider applications, Turnstile widget, deployment, and live Resend acceptance.
- CMS, catalogue/provider writes, orders, membership operations, users, media uploads, settings persistence, and integrations.
- Pages project creation, deployment, custom domain, DNS, analytics, and production acceptance.

## 2026-08-29 — first-party catalogue media and modal editors

- Copied all 464 unique current catalogue images (466 product image references across 49 public products) into immutable, content-addressed Admin R2 objects and replaced the Commerce D1 browser projection with the first-party `/commerce-media/*` route. Product image edits now ingest validated JPG/PNG/WebP HTTPS sources into R2 before merchandising data is saved.
- Replaced the below-list Product and Collection editors with responsive lightbox dialogs that trap focus, close with Escape or themed controls, restore trigger focus, lock background scrolling, and scroll internally. Edit, Manage, Close, Save, Archive, Delete, and reorder controls now follow the black-and-gold page hierarchy instead of generic grey button styling.
- Deployed Admin Production as `df134df3` (`https://df134df3.thirdrailify-admin.pages.dev`). No Public code deployment, Printful call, Wix write, DNS change, checkout activation, payment change, or fulfillment action occurred.

## 2026-08-29 — permanent commerce production controls

- Added the real business/tax/template/document administration model, encrypted private-field custody, masked tax lists, safe template variables and previews, deterministic Resend delivery ledger, paid TEST receipt rendering, opaque receipt-token architecture, and a centralized derived production-readiness projection.
- Preserved the accepted Stripe TEST order and all disabled checkout/live-payment/fulfillment gates. Printful migration execution and every Printful/Wix/DNS write remain outside this milestone.

## 2026-08-29 — Canadian legal operations support

- Added `COMMERCE_SUPPORT_RUNBOOK.md` with the exact current order lookup, payment/webhook, product mapping and fulfilment evidence plus explicit gaps for refunds, cancellations, replacements, retries, missing/damaged/wrong shipments, disputes, notifications and Printful claims. It grants no remedy or provider-write authority.
- Added a Master-only, exact-origin, CSRF-protected expired-auth maintenance action. It deletes only sessions, handoffs, OAuth transactions, email-verification tokens and password-reset tokens whose pre-existing `expires_at` is at or before execution; it preserves accounts, identities, rate-limit rows and audit history and records a bounded cleanup audit event.
- Added exact expiry-boundary coverage using the local D1 harness. No real email, refund, checkout, fulfilment, Printful migration, Wix, secret, DNS, domain or production-routing action is part of this change.
- Deployed only the two auth-maintenance runtime files from an isolated clean release tree. Production deployment `2ce3b918` is live at `https://2ce3b918.thirdrailify-admin.pages.dev`; both the immutable and stable Admin origins return `401 unauthenticated` for the cleanup route without a valid session, and no cleanup was executed during acceptance.
- Polished the Site Content banner editor: the `Open Public site` label and external-link icon now share a centred flex row with balanced padding and a full-width mobile treatment; panels, fields, empty/loading states, toggles, previews and the save rail received scoped visual refinement; checkbox labels stay inline; and destructive message removal no longer inherits the gold primary fill.
- Deployed the isolated Content-page polish as Admin Production deployment `a603f8eb` (`https://a603f8eb.thirdrailify-admin.pages.dev`). The immutable and stable `/content` routes return `200` with matching `index-t2baMVKy.js` and `index-Qrrq_Twt.css` assets, and the previously released auth-cleanup route still fails closed with `401` when unauthenticated.
